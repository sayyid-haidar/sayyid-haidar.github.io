# Content authoring

Add Markdown files to one of these folders:

- `projects/`
- `cheatsheets/`
- `writing/`

Filenames become URL slugs and must use lowercase kebab-case. New content is a
draft unless frontmatter explicitly sets `draft: false`.

Run `npm run validate:content` before previewing or committing content.
